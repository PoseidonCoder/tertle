import { View } from "react-native";

const divider = () => (
	<View
		style={{
			borderBottomWidth: 1,
			borderColor: "lightgray",
			margin: 10,
			width: "100%",
		}}
	/>
);

export default divider;
